<?xml version="1.0" encoding="UTF-8" ?>
<!-- XML约束头:MyBatis的约束文件使用的dtd，规范这个XML文档处理 遵守XML的一般规则以外，要遵守的自定义规则:能够使用哪些标签，使用哪些属性，出现的次数，标签的先后属性 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapper是映射文件的根节点 namespace属性：名称空间，命名空间.就是一个名字 现在可以任意，以后使用接口的全限定名 -->
<mapper namespace="com.woniuxy.smart_community.dao.ParkingLotDao">



<!--查询-->
    <select id="selectAllParkingLot" resultType="com.woniuxy.smart_community.entity.ParkingLot">
        select * from parking_lot
    </select>
    
    <select id="selectParkingLotById" resultType="com.woniuxy.smart_community.entity.ParkingLot">
        select * from parking_lot where id=#{id}
    </select>
    
    <select id="selectParkingLotByName" resultType="com.woniuxy.smart_community.entity.ParkingLot">
        select * from parking_lot where parking_lot_name like '%' #{parkingName} '%'
    </select>
    
    


    
<!--添加-->
    <insert id="insertParkingLot">
        insert into parking_lot values(null,#{parkingLot.parking_lot_name},#{parkingLot.park_nums},
        #{parkingLot.sell_park_nums},#{parkingLot.hire_park_nums},#{parkingLot.now_use_nums},1)
    </insert>


<!--修改-->
    <update id="updateParkingLot">
        update parking_lot set
        <include refid="updateParkingLot"/>
        where id=#{parkingLot.id}
    </update>

    <update id="updateParkingLotState">
        update parking_lot  set state=#{state} where id=#{id}
    </update>


<!--删除-->



<!--共用功能-->
    <!--状态修改需要更高权限，单独方法-->
    <!--对象属性修改-->
    <sql id="updateParkingLot">
            <if test="parkingLot.parking_lot_name!=null and parkingLot.parking_lot_name!=''">
                parking_lot_name=#{parkingLot.parking_lot_name}
            </if>
            <if test="parkingLot.park_nums!=null and parkingLot.park_nums!=''">
                ,park_nums=#{parkingLot.park_nums}
            </if>
            <if test="parkingLot.sell_park_nums!=null and parkingLot.sell_park_nums!=''">
                ,sell_park_nums=#{parkingLot.sell_park_nums},
            </if>
            <if test="parkingLot.hire_park_nums!=null and parkingLot.hire_park_nums!=''">
                ,hire_park_nums=#{parkingLot.hire_park_nums}
            </if>
            <if test="parkingLot.now_use_nums!=null and parkingLot.now_use_nums!=''">
                ,now_use_nums=#{parkingLot.now_use_nums}
            </if>
<!--            <if test="parkingLot.state!=null">-->
<!--                ,state=#{parkingLot.state}-->
<!--            </if>-->
    </sql>

</mapper>
